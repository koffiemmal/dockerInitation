sudo: required
services:
  - docker

script:

    - docker build -t backend .
    - docker images backend

before_deploy: 
    - echo "AZemmanuel20" | docker login -u messanemmanuel --password-stdin

deploy:
     provider: script
     script: docker push backend
     on:
        branch: main
